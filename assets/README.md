# Assets

This task does not rely on a static external image bank.

- Matrix panels, answer cards, and shape tokens are rendered at runtime with PsychoPy primitives.
- Practice and feedback screens are text-only and are configured in `config/*.yaml`.
- The generated figure preview lives at `task_flow.png` in the task root.
